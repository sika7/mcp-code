log_path: "{{TEST_DIR}}/logs"
excluded_files:
  - "**/*.pem"
  - "**/*.key"

current_project: "project1"
projects:
  project1:
    src: "{{TEST_DIR}}/project1"
    scripts:
      build: "npm run build"
      test: "npm run test"
    excluded_files:
      - "**/.env"
  project2:
    src: "{{TEST_DIR}}/project2"
    scripts:
      start: "npm start"
      deploy: "npm run deploy"
    excluded_files:
      - "dist/**"
      - "*.log"
